# include  <bits/stdc++.h>
# ifdef LOCAL
    # include "C:\Users\Kevin\Desktop\demo\C++_code\debug.h"
#endif
using  namespace  std ;

namespace predefine {
    const long long inf = 0x3f3f3f3f3f3f3f3f ;
    using ll = long long ;using i64 = long long ;using Long = long long ;
    using Int = __int128 ;using pll = pair < Long , Long > ;

    # define af( i , a , b ) for ( Long i = a ; i <= b ; ++ i )
    # define df( i , a , b ) for ( Long i = a ; i >= b ; -- i )

    # define endl '\n'
}
using namespace predefine ;

//-----  板子  ---------------------------------------------------------------------------------------------------------

//-----  板子  ---------------------------------------------------------------------------------------------------------

inline Long sum1 ( Long _a ) { return ( _a * ( _a + 1 ) ) >> 1 ; }
inline Long sum2 ( Long _b ) { return _b * ( _b + 1 ) * ( 2 * _b + 1 ) / 6  ; }

Long dx [ 4 ] = { -1 , 0 , 1 , 0 } ;
Long dy [ 4 ] = { 0 , 1 , 0 , -1 } ;

void solve () {
}
/*
*/
int  main () {
# ifndef cin or cout
    std::ios::sync_with_stdio (false);
    std::cin.tie (nullptr) ;
# endif

//	__fin ( "in.in" ) ;
	ll _ = 1 ;
	// rd ( _ ) ;
 	// cin >> _ ;
	while ( _ -- ) {
        // Dgop ;
        solve () ;
        // Dged ;
	}
	return 0 ;
}

/*
//-----  数据  ---------------------------------------------------------------------------------------------------------

----------
*/


